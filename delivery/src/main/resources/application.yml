server:
    port:8080

spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/delivery_tracking
        username: postgres
        password: 123123
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                format_sql: true